{
  "name": "valuate-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling for the Valuate addon: static gates plus a fengari runtime harness.",
  "scripts": {
    "check": "node gates.js",
    "test": "node gates.js",
    "install-hooks": "node -e \"require('fs').copyFileSync('hooks/pre-commit','../.git/hooks/pre-commit')\""
  },
  "dependencies": {
    "fengari": "^0.1.5",
    "luaparse": "^0.3.1"
  }
}
